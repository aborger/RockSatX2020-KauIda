import threading
import os
from arduino_communication import *

#Rf = threading.Thread(target=RF)
#Serial = threading.Thread(target=serial)

#Rf.start()
#Serial.start()

#Rf.join()
os.system("python rfExperiment.py")
#Serial.join()
